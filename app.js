class MobileNavBar {
    constructor(inteface) {
        this.menu-desktop = document.querySelector(menu-desktop);
        this.mobile-button = ddocument.querySelector(mobile-button);
    }
    addClickEvent() {
        this.mobile-button.addEventListener("Click")
    };
}